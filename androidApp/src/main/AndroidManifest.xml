<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ConiferApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <!-- The app draws edge to edge and handles the IME itself via imePadding(); without an
             explicit mode the system is free to pan the whole window up for the keyboard, which
             stacks on top of that padding and leaves a large gap above the IME. -->
        <activity
            android:name=".ConiferActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver
            android:name=".ConversationBroadcastReceiver"
            android:exported="false" />
        <!-- Lets the share sheet read a crash report out of the app's own cache folder, and only
             that folder (see res/xml/file_paths.xml) - a private file cannot otherwise be handed to
             another app at all. The authority matches what AndroidReportShareController asks for,
             which builds it from the package name. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>